import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.smartlist.app',
  appName: 'SmartList',
  webDir: 'dist/smart-list/browser'
};

export default config;
